name: Put next tag

on:
  push:
    branches:    
      - master

jobs:
  tag:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Github Action for LaTeX
        run: ./calcVersion.sh
